# Gradient Clipping

!!! warning "Under Construction"
    This page will be updated as the clipping module is implemented.

## Overview

Per-example gradient clipping is the first step in DP-SGD. It bounds the sensitivity of the gradient computation.

## Coming Soon

- `clipped_grad()` API usage
- Understanding clip norms
- Microbatching for memory efficiency
