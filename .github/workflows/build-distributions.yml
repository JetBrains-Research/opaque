name: Build distributions

on:
  workflow_call:
    inputs:
      artifact-prefix:
        description: Prefix for all uploaded build artifacts.
        required: true
        type: string
      checkout-ref:
        description: Ref to build. Leave empty to build the triggering SHA.
        required: false
        default: ""
        type: string
      retention-days:
        description: Number of days to retain uploaded artifacts.
        required: true
        type: number
      version:
        description: Explicit build version. Leave empty to derive it from git.
        required: false
        default: ""
        type: string

jobs:
  build-python-wheels:
    name: Python packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0
          ref: ${{ inputs.checkout-ref || github.sha }}

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.11"

      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: false

      - name: Preflight build version
        run: bash .github/scripts/set_build_versions.sh "${{ inputs.version }}"

      - name: Build opaque
        run: uv build . --wheel --out-dir dist

      - name: Build opaque-base
        run: uv build packages/opaque-base --wheel --out-dir dist

      - name: Build opaque-engine
        run: uv build packages/opaque-engine --wheel --out-dir dist

      - name: Build opaque-optimizers
        run: uv build packages/opaque-optimizers --wheel --out-dir dist

      - name: Build opaque-dpsgd
        run: uv build packages/opaque-dpsgd --wheel --out-dir dist

      - name: Build opaque-dpftrl
        run: uv build packages/opaque-dpftrl --wheel --out-dir dist

      - name: Build opaque-auditing
        run: uv build packages/opaque-auditing --wheel --out-dir dist

      - name: Build opaque-patches
        run: uv build packages/opaque-patches --wheel --out-dir dist

      - name: Build opaque-transformers
        run: uv build packages/opaque-transformers --wheel --out-dir dist

      - name: Build opaque-alignment
        run: uv build packages/opaque-alignment --wheel --out-dir dist

      - name: Validate wheel metadata
        run: >-
          uv run --isolated --no-project --with packaging==25.0
          python .github/scripts/check_wheel_internal_pins.py --wheel-dir dist

      - name: Upload wheels
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: ${{ inputs.artifact-prefix }}-python
          path: dist/*.whl
          retention-days: ${{ inputs.retention-days }}

  build-native-artifacts:
    name: opaque-accounting (${{ matrix.target.label }})
    runs-on: ${{ matrix.target.runner }}
    strategy:
      fail-fast: false
      matrix:
        target:
          - kind: wheel
            label: Linux x86_64
            artifact: linux-x86_64
            pattern: "*.whl"
            runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            manylinux: 2_28
          - kind: wheel
            label: Linux arm64
            artifact: linux-arm64
            pattern: "*.whl"
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            manylinux: 2_28
          - kind: wheel
            label: macOS arm64
            artifact: macos-arm64
            pattern: "*.whl"
            runner: macos-latest
            target: aarch64-apple-darwin
            manylinux: off
          - kind: sdist
            label: sdist
            artifact: sdist
            pattern: "*.tar.gz"
            runner: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0
          ref: ${{ inputs.checkout-ref || github.sha }}

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.11"

      - if: matrix.target.kind == 'wheel'
        uses: ./.github/actions/setup-rust

      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: false

      - name: Preflight build version
        run: bash .github/scripts/set_build_versions.sh "${{ inputs.version }}"

      - name: Build wheel
        if: matrix.target.kind == 'wheel'
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1
        with:
          command: build
          working-directory: packages/opaque-accounting
          target: ${{ matrix.target.target }}
          manylinux: ${{ matrix.target.manylinux }}
          args: >-
            --release
            --interpreter python3.11
            --out ../../dist
            --compatibility pypi

      - name: Build sdist
        if: matrix.target.kind == 'sdist'
        run: uv build packages/opaque-accounting --sdist --out-dir dist

      - name: Validate wheel metadata
        if: matrix.target.kind == 'wheel'
        run: >-
          uv run --isolated --no-project --with packaging==25.0
          python .github/scripts/check_wheel_internal_pins.py --wheel-dir dist

      - name: Import native extension from wheel
        if: matrix.target.kind == 'wheel'
        run: |
          wheels=(dist/*.whl)
          if [[ "${#wheels[@]}" -ne 1 ]]; then
            echo "::error::Expected one wheel, found ${#wheels[@]}"
            exit 1
          fi
          python3 -m venv wheel-import
          wheel-import/bin/python -m pip install --no-deps "${wheels[0]}"
          wheel-import/bin/python - <<'PY'
          import ctypes
          from pathlib import Path

          native = next(Path("wheel-import").glob("**/opaque_accounting.abi3.so"))
          ctypes.CDLL(str(native))
          PY

      - name: Validate artifact policy
        run: python3 .github/scripts/check_accounting_artifact_policy.py --wheel-dir dist

      - name: Rebuild wheel from sdist
        if: matrix.target.kind == 'sdist'
        run: |
          sdists=(dist/*.tar.gz)
          if [[ "${#sdists[@]}" -ne 1 ]]; then
            echo "::error::Expected one sdist, found ${#sdists[@]}"
            exit 1
          fi
          uv build "${sdists[0]}" --wheel --out-dir rebuilt

      - name: Validate rebuilt wheel artifact policy
        if: matrix.target.kind == 'sdist'
        run: python3 .github/scripts/check_accounting_artifact_policy.py --wheel-dir rebuilt

      - name: Upload artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: ${{ inputs.artifact-prefix }}-opaque-accounting-${{ matrix.target.artifact }}
          path: dist/${{ matrix.target.pattern }}
          retention-days: ${{ inputs.retention-days }}
