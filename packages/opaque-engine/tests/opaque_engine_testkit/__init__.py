"""Repository-only helpers for testing Opaque engine providers.

This package deliberately lives under ``opaque-engine/tests`` so it is not
included in the ``opaque-engine`` runtime distribution.
"""
